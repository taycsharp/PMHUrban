services:
  backend:
    volumes:
      - ./backend:/app
    command: sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"

  frontend:
    command: npm run dev
    volumes:
      - ./frontend:/app
      - /app/node_modules

